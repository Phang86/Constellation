<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <group
        android:checkableBehavior="single">

        <item
            android:id="@+id/gongneng"
            app:showAsAction="always"
            app:actionLayout="@layout/menu_gongneng"
            tools:ignore="MenuTitle"
            />

        <item
            android:id="@+id/weather"
            app:showAsAction="always"
            app:actionLayout="@layout/menu_weather"
            tools:ignore="MenuTitle"
            />

        <item
            android:id="@+id/news"
            app:showAsAction="always"
            app:actionLayout="@layout/menu_news"
            tools:ignore="MenuTitle"
            />


        <item
            android:id="@+id/xingshi"
            app:showAsAction="always"
            app:actionLayout="@layout/menu_dict"
            tools:ignore="MenuTitle"
            />

        <item
            android:id="@+id/tally"
            app:showAsAction="always"
            app:actionLayout="@layout/menu_tally"
            tools:ignore="MenuTitle"
            />

        <item
            android:id="@+id/user_info"
            app:showAsAction="always"
            app:actionLayout="@layout/menu_appinfo"
            tools:ignore="MenuTitle"
            />

        <item
            android:id="@+id/aboutMe"
            app:showAsAction="always"
            app:actionLayout="@layout/menu_about_me"
            tools:ignore="MenuTitle"
            />

        <item
            android:id="@+id/exit"
            app:showAsAction="always"
            app:actionLayout="@layout/menu_exit"
            tools:ignore="MenuTitle"
            />

    </group>

</menu>